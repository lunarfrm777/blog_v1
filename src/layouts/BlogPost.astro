---
import SubscribeForm from '../components/SubscribeForm.astro';
const { frontmatter } = Astro.props;
---
<html lang="en">
  <head>
    <title>{frontmatter.title}</title>
  </head>
  <body style={`background-color: ${frontmatter.backgroundColor || '#FCBBDC'}`}>
    <main>
      <slot />
    </main>
     <SubscribeForm />
  </body>
</html>
 